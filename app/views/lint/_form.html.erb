<%= form_for(@lint) do |f| %>
  <% if @lint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lint.errors.count, "error") %> prohibited this lint from being saved:</h2>

      <ul>
      <% @lint.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
